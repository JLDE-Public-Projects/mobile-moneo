/** Props comunes a todos los íconos de la app. */
export interface IconProps {
  /** Tamaño (ancho y alto) en px. Por defecto 25, como en el diseño. */
  size?: number;
  /** Color del trazo. Por defecto el color de acento. */
  color?: string;
}
